package com.querymind.connection.domain;

public enum DatabaseType {
    MYSQL
    // POSTGRES, SQLSERVER, ORACLE added later behind DatabaseProvider — zero
    // business-logic changes required (memory.md §2).
}
