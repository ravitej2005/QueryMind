spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:querymind}?useSSL=false&allowPublicKeyRetrieval=true
    username: ${DB_USER:querymind}
    password: ${DB_PASSWORD:querymind}
logging:
  level:
    com.querymind: DEBUG
